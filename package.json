{
  "name": "teacher-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently -n \"BACKEND,FRONTEND\" -c \"cyan,magenta\" \"npm run start --prefix Teacher-backend\" \"npm run dev --prefix Teacher-frontend\"",
    "student": "concurrently -n \"STUDENT-API,STUDENT-UI\" -c \"blue,green\" \"npm run start --prefix Student-backend\" \"npm run dev --prefix Student-frontend\"",
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"cyan,magenta\" \"npm run dev --prefix Teacher-backend\" \"npm run dev --prefix Teacher-frontend\"",
    "backend": "npm start --prefix Teacher-backend",
    "frontend": "npm run dev --prefix Teacher-frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
